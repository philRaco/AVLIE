///avlie_wait_one_frame()

actions[c, ++i] = event.wait_one_frame;
action_event[c, i] = true; //any of then action enum values

print("    instruction", i);
print("    type", event.wait_one_frame);
print("")
