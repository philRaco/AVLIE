///avlie_wait_until_timer()

actions[c, ++i] = event.wait_until_timer;
action_event[c, i] = true; //any of then action enum values

print("    instruction", i);
print("    type", event.wait_until_timer);
print("")
